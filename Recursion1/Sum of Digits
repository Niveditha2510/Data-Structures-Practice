#include <iostream>
#include "solution.h"
using namespace std;

int sumOfDigits(int n) {

    if(n==0)
        return 0;
    int r=n%10;
    int ans=r+sumOfDigits(n/10);
}

int main() {
   int n;
   cin >> n;
   cout << sumOfDigits(n) << endl;
}
